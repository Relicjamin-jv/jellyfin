apiVersion: v1
kind: Service
metadata:
  name: jellyfin-svc
  namespace: default
spec:
  selector:
    app: {{ .Release.Name }} 
  type: LoadBalancer
  ports:
    - name: jellyfin-port
      protocol: TCP
      port: 8906 
      targetPort: jellyfin-port
---

